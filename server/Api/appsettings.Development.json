{
  "ConnectionStrings": {
    "PrimaryConnection": "Host=${PRIMARY_DB_HOST};Port=${PRIMARY_DB_PORT};Database=${PRIMARY_DB_NAME};Username=${PRIMARY_DB_USER};Password=${PRIMARY_DB_PASSWORD}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JwtSettings": {
    "Secret": "this-is-a-very-secure-and-long-key-32-bytes-long",
    "ExpirationTimeInMinutes": 60000,
    "Issuer": "HighloadSocial",
    "Audience": "HighloadSocial"
  }
}
